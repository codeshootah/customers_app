<h2>All Customers</h2>
  <% @customers.each do |customer| %>

    <div>
      <h3><%= customer.name %></h3>
      <p><%= customer.phone %></p>
      <p><%= customer.description %></p>
    </div>

  <% end %>

<%= link_to 'Black List', black_list_path %>